import React from 'react';

const Navigation = () => {
  return (
    <div style={styles.nav}>
      Nav is here
    </div>
  );
};

const styles = {
  nav: {
    backgroundColor: '#1976d2',
    color: 'white',
    padding: '16px',
    textAlign: 'center',
    fontSize: '24px',
  },
};

export default Navigation;
